"""Placeholder test suite — replace with real tests as the project grows."""

import Klaus_proxy_local


def test_version() -> None:
    """Package is importable and exposes __version__."""
    assert Klaus_proxy_local.__version__ == "0.1.0"
